# 汉字 Wordle 计时功能视觉设计方案

## 一、参考网站分析

### 1. handle.antfu.me 计时功能视觉特点

**位置布局**：
- 位于页面顶部标题区域
- 标题下方，居中显示
- 与标题保持适当间距

**视觉样式**：
- 数字字体：等宽字体
- 颜色：红色（醒目）
- 格式：MM:SS 格式
- 大小：适中，与标题形成层次
- 动画：秒数变化时有轻微的视觉反馈

**交互体验**：
- 游戏开始时自动启动
- 游戏结束时停止并保持最终时间
- 重新开始时重置

## 二、本项目视觉设计方案

### 1. 整体布局

**位置**：
- 位于 `header` 组件内
- 标题 `<h1>` 和副标题 `<p>` 之间
- 居中显示

**结构**：
```jsx
<header className="w-full text-center py-4 sm:py-5 border-b border-gray-200 px-4">
  <h1 className="text-2xl sm:text-3xl font-bold text-gray-900 mb-1">汉字 Wordle</h1>
  {/* 计时器位置 */}
  <div className="timer-container">
    <span className="timer">00:00</span>
  </div>
  <p className="text-sm sm:text-base text-gray-600">猜四字成语</p>
  {/* 其他按钮 */}
</header>
```

### 2. 视觉样式

**颜色方案**：
- 主色：`#ef4444`（红色，与错误提示一致）
- 背景：透明（与 header 背景一致）
- 边框：无
- 阴影：无

**字体样式**：
- 字体：`font-mono`（等宽字体，数字对齐）
- 大小：`text-lg sm:text-xl`（响应式）
- 粗细：`font-semibold`（醒目）
- 间距：`tracking-wider`（数字间距适中）

**动画效果**：
- 秒数变化：轻微的缩放动画
- 停止状态：轻微的高亮效果
- 过渡：`transition-all duration-200`（平滑过渡）

### 3. 响应式设计

**断点设计**：
- 移动端（< 640px）：`text-lg`
- 平板/桌面（≥ 640px）：`text-xl`

**间距调整**：
- 移动端：`mb-1 mt-1`
- 平板/桌面：`mb-1.5 mt-1.5`

### 4. 状态样式

**运行状态**：
- 颜色：`#ef4444`（红色）
- 动画：秒数变化时 `scale-105` 然后恢复

**停止状态**：
- 颜色：`#ef4444`（保持红色）
- 效果：添加 `opacity-90` 效果

**重置状态**：
- 颜色：`#ef4444`（红色）
- 动画：`scale-95` 然后恢复到 `scale-100`

## 三、具体实现代码

### 1. CSS 样式

```css
/* App.css */
.timer-container {
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0.25rem 0;
}

.timer {
  font-family: monospace;
  font-size: 1.125rem;
  font-weight: 600;
  color: #ef4444;
  letter-spacing: 0.05em;
  transition: all 0.2s ease;
}

@media (min-width: 640px) {
  .timer {
    font-size: 1.25rem;
  }
  
  .timer-container {
    margin: 0.375rem 0;
  }
}

.timer.running {
  /* 运行状态样式 */
}

.timer.stopped {
  opacity: 0.9;
}

.timer.animating {
  transform: scale(1.05);
}
```

### 2. React 组件实现

```jsx
// 计时器组件
function Timer({ time, isRunning }) {
  const [isAnimating, setIsAnimating] = useState(false);
  const [lastTime, setLastTime] = useState(time);

  // 检测时间变化，触发动画
  useEffect(() => {
    if (time !== lastTime) {
      setIsAnimating(true);
      setTimeout(() => setIsAnimating(false), 200);
      setLastTime(time);
    }
  }, [time, lastTime]);

  return (
    <div className="timer-container">
      <span className={`timer ${isRunning ? 'running' : 'stopped'} ${isAnimating ? 'animating' : ''}`}>
        {time}
      </span>
    </div>
  );
}

// 在 App.tsx 中使用
<header className="w-full text-center py-4 sm:py-5 border-b border-gray-200 px-4">
  <h1 className="text-2xl sm:text-3xl font-bold text-gray-900 mb-1">汉字 Wordle</h1>
  <Timer 
    time={formattedTime} 
    isRunning={timerIsRunning} 
  />
  <p className="text-sm sm:text-base text-gray-600">猜四字成语</p>
  {/* 其他按钮 */}
</header>
```

## 四、视觉效果对比

| 元素 | 参考网站 | 本项目实现 | 对比结果 |
|------|---------|-----------|----------|
| **位置** | 标题下方居中 | 标题下方居中 | 一致 |
| **颜色** | 红色 | 红色 (#ef4444) | 一致 |
| **字体** | 等宽字体 | 等宽字体 (font-mono) | 一致 |
| **格式** | MM:SS | MM:SS | 一致 |
| **大小** | 适中 | 响应式大小 | 优化 |
| **动画** | 轻微动画 | 缩放动画 | 优化 |
| **响应式** | 固定大小 | 响应式设计 | 优化 |

## 五、用户体验考量

### 1. 可读性
- 等宽字体确保数字对齐
- 红色醒目但不刺眼
- 响应式设计适应不同屏幕

### 2. 交互反馈
- 秒数变化有动画反馈
- 停止状态有视觉提示
- 重置时有过渡动画

### 3. 视觉层次
- 计时器位于标题和副标题之间
- 字体大小适中，不喧宾夺主
- 与现有界面风格统一

### 4. 可访问性
- 颜色对比度符合 WCAG 标准
- 文本大小可调整
- 无闪烁效果（动画持续时间短）

## 六、实现注意事项

### 1. 性能优化
- 避免频繁 DOM 操作
- 使用 CSS 动画而非 JavaScript 动画
- 游戏结束后停止动画

### 2. 兼容性
- 等宽字体在各浏览器中表现一致
- 动画效果在旧浏览器中优雅降级
- 响应式设计适配不同设备

### 3. 维护性
- 样式模块化，便于调整
- 组件化设计，便于复用
- 代码注释清晰，便于理解

## 七、总结

本视觉设计方案参考了 handle.antfu.me 的计时功能实现，同时结合本项目的现有风格进行了优化。通过精心的布局设计、颜色选择和动画效果，确保计时器既醒目又不突兀，为用户提供直观的时间反馈。

方案考虑了响应式设计、用户体验和可访问性，确保在不同设备和浏览器上都能正常工作。通过组件化实现，便于代码维护和功能扩展。